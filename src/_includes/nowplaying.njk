---
layout: "base.njk"
---
<section>
    {{content | safe }}
</section>
    <hr>
    <article class="grid-test">
        {% for post in collections.nowplaying %}
            <section>
            <h2>{{ post.data.title }}</h2>
            {{ post.templateContent | safe }}
            <br>
            {{ post.date | postDate }}
            </section>
        {% endfor %}
    </article>